LangChain 是一个用于构建基于大语言模型应用的开发框架。

RAG 的核心思路是先检索，再生成。先从本地知识中找出相关内容，再把内容交给模型组织成答案。

文档切分的目的，是让长文本变成更容易检索的小片段。切分太大，召回不准；切分太小，可能丢失上下文。

向量检索适合语义相关的问题。查询和原文不完全一致时，语义相似度仍然可以把相关片段找出来。

在真实项目里，RAG 常用于企业知识库、产品文档问答和内部资料搜索。
