LangChain 是一个帮助开发者构建大语言模型应用的开发框架。

它并不是大模型本身，而是围绕大模型应用开发提供一层组织能力。

常见能力包括：
- Prompt 模板管理
- 聊天模型调用
- 输出解析
- 结构化输出
- 文档切分
- 检索增强生成
- 工具调用
- Agent 编排

如果只是一个非常简单的聊天问答，直接调用模型 API 往往就够了。
但当系统开始涉及知识库、多轮对话、结构化结果、外部工具和多步骤流程时，工程复杂度会明显上升。

LangChain 的价值就在于给这些常见场景提供一套相对统一的开发方式。
